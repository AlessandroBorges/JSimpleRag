<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <!--
    NOTE: This file was intentionally left minimal.

    Triggers previously defined here were removed because:

    1. text_search_tsv trigger: REDUNDANT
       - The field is already a GENERATED COLUMN in the DDL (003-create-tables.xml)
       - PostgreSQL automatically updates it when texto or metadados change

    2. updated_at triggers: REDUNDANT
       - Managed by JPA @PreUpdate in the Updatable superclass
       - See: src/main/java/bor/tools/simplerag/entity/Updatable.java
       - Using both JPA and DB triggers could cause conflicts

    If you need custom triggers in the future, add them here as new changesets.
    -->

</databaseChangeLog>